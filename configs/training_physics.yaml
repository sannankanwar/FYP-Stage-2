# Training Configuration (Physics Informed Loss)
experiment_name: null # e.g. "experiment_physics"
description: ""

seed: 42
epochs: 100
learning_rate: 0.001
optimizer: "adam"
loss_function: "physics_informed"
log_interval: 10
checkpoint_dir: "outputs/checkpoints_physics"
log_dir: "outputs/logs_physics"
