# Phase 2 B11: FNO-ResNet18 + SGD + Cosine
experiment_name: "exp2_B11_resnet18_sgd_cosine"
description: "Optimizer sweep: FNO-ResNet18 with SGD momentum and Cosine Annealing."

model:
  name: "fno_resnet18"
  output_dim: 5
  modes: 32

training:
  epochs: 250
  batch_size: 32
  learning_rate: 0.01
  optimizer: "sgd"
  momentum: 0.9
  scheduler: "cosine"

data:
  resolution: 256
  train_samples: 2000
  val_samples: 400

grid_strategy: "random_all"
standardize_outputs: true
loss_function: "weighted_standardized"
loss_weights: [1.0, 1.0, 1.0, 10.0, 10.0]
