# Model Configuration
name: "spectral_resnet"
input_channels: 2  # [cos(phase), sin(phase)]
output_dim: 5      # [xc, yc, fov, wavelength, focal_length]
modes: 16            # FFT modes to keep
hidden_dims: [64, 64, 64] # Legacy (optional)
